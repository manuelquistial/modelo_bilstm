# Paperspace / GPU: install PyTorch with CUDA first, then this file.
# Example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-paperspace.txt

-r requirements.txt

# Headless: no display for pygame acquisition
# pygame still listed in requirements.txt for optional local acquisition
